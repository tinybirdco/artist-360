
SCHEMA >
    `external_id` LowCardinality(String),
    `play_time` DateTime,
    `country` LowCardinality(String),
    `source` LowCardinality(String)

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toDate(play_time)"
ENGINE_SORTING_KEY "play_time, cityHash64(external_id)"
ENGINE_SAMPLING_KEY "cityHash64(external_id)"
